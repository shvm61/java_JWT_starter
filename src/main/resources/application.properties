# database url with with legace time =false
spring.datasource.url=jdbc:mysql://localhost:3306/bookmyshow?useLegacyDatetimeCode=false
# save time in UTC in db
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

spring.datasource.username=root
spring.datasource.password=mahakal
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=8000
# spring.datasource.maxActive=2

# auto create schema
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto=update


# write sql in console
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# ignore null values in response
spring.jackson.default-property-inclusion=non_null

# debug mode similar to morgan
logging.level.org.springframework.web=DEBUG

# color logging
spring.output.ansi.enabled=always

